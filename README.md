# vite-react-tsswc-template
This template was create by running `npm create vite@latest`
and choosing react + typescript + SWC

It also includes a devcontainer setting and npm scripts for publishing to gcs